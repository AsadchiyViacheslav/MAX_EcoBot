# ЭкоБот

Чат-ьот для мессенджера MAX для повышения экологической осведомленности жителей с возможность поиска пунктов приёма вторсырья, эко-френдли заведений и экологической информации.

## Функциональность

- 🔍 Поиск пунктов приёма вторсырья по городу
- 🌿 Поиск эко-френдли заведений
- 💚 Информация о благотворительных организациях
- 📚 Образовательные материалы по экологии
- 📍 Определение местоположения через геолокацию

## Запуск проекта

### Предварительные требования

- Docker
- Токен чат-бота MAX

### Запуск

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd <project-directory>
```

2. **Создайте файл окружения**
Через консоль
```bash
echo "BOT_TOKEN=your_MAX_bot_token" > .env
```
Либо вручную через файловую систему

3. **Сборка образа**
```bash
docker build -t eco-bot .
```

4. **Запуск контейнера**
```bash
docker run -d --name eco-bot .env eco-bot
```

5. **Использование**

В мессенджере MAX найти бота можно по имени `t138_hakaton_bot`

```bash
# Просмотр логов
docker logs eco-bot

# Удаление контейнера
docker rm eco-bot
```