# Эко-Бот

Чат-ьот для мессенджера MAX для повышения экологической осведомленности жителей с возможность поиска пунктов приёма вторсырья, эко-френдли заведений и экологической информации.

## Функциональность

- 🔍 Поиск пунктов приёма вторсырья по городу
- 🌿 Поиск эко-френдли заведений
- 💚 Информация о благотворительных организациях
- 📚 Образовательные материалы по экологии
- 📍 Определение местоположения через геолокацию

## Запуск проекта

### Предварительные требования

- Docker
- Токен чат-бота MAX

### Запуск

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd <project-directory>
```

2. **Создайте файл окружения**
Через консоль
```bash
echo "BOT_TOKEN=your_MAX_bot_token" > .env
```
Либо вручную через файловую систему

3. **Сборка образа**
```bash
docker build -t eco-bot .
```

4. **Запуск контейнера**
```bash
docker run -d --name eco-bot .env eco-bot
```

5. **Использование**

В мессенджере MAX найти бота можно по имени `t138_hakaton_bot`

```bash
# Просмотр логов
docker logs eco-bot

# Удаление контейнера
docker rm eco-bot
```

## Структура проекта

```
// Ядро бота
bot.ts - Основная логика, роутинг событий

// Обработчики
handlers/
  ├── recycling.ts  - Пункты приёма вторсырья
  ├── ecoplaces.ts  - Эко-заведения  
  ├── info.ts       - Образовательный контент
  └── charity.ts    - Благотворительность

// Сервисы
db.ts           - Работа с базой данных
materials.ts    - Локализация материалов
menus.ts        - Генерация клавиатур

// Данные
info/           - Markdown контент
recycle.db      - SQLite база пунктов
```

*Проект использует только открытые API и свободно распространяющиеся данные*